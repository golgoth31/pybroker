---
active: yes
inputs:
    active: yes
    name: external
    workers: 1
    links:
        out: yes
        in: no
    options:
        # driver: zmq
        type: tcp
        bind_address: 127.0.0.1
        bind_port: 10250
caches:
  active: no

filters:
  active: no

outputs:
  active: no
